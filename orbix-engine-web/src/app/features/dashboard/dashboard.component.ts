import { Component } from '@angular/core';

@Component({
  selector: 'orbix-dashboard',
  standalone: true,
  template: `
    <h2 class="h3 mb-4">Dashboard</h2>
    <p class="text-muted">Scaffold landing page. Replace with branch KPIs, stock alerts, debt ageing, sync health.</p>
  `
})
export class DashboardComponent {}
