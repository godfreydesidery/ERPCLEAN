import 'package:flutter/material.dart';

class TillCloseScreen extends StatelessWidget {
  const TillCloseScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('TillCloseScreen')),
      body: const Center(child: Text('Scaffold placeholder — see USER-STORIES.md POS epic.')),
    );
  }
}
