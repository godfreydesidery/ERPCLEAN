import 'package:flutter/material.dart';

class TillOpenScreen extends StatelessWidget {
  const TillOpenScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('TillOpenScreen')),
      body: const Center(child: Text('Scaffold placeholder — see USER-STORIES.md POS epic.')),
    );
  }
}
