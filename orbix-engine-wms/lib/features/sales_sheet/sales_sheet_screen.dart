import 'package:flutter/material.dart';

class SalesSheetScreen extends StatelessWidget {
  const SalesSheetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('SalesSheetScreen')),
      body: const Center(child: Text('Scaffold placeholder — see USER-STORIES.md WMS epic.')),
    );
  }
}
