# QA deploy target — non-secret config, read by deploy.ps1 / deploy.sh.
# The SSH key path and any GitHub PAT are NOT here — pass those via env
# ($env:ORBIX_SSH_KEY / $env:GH_PAT) or a gitignored deploy.env.local.

EC2_HOST=16.170.11.41
EC2_USER=ubuntu
BRANCH=main
REMOTE_DIR=orbix
CONTAINER=orbix
IMAGE=orbix:qa
DB_VOLUME=orbix-data
