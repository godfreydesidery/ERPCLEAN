package com.orbix.engine.modules.admin.domain.enums;

/** Shared lifecycle status for admin master entities (org / company / branch / section / currency). */
public enum AdminStatus { ACTIVE, INACTIVE, ARCHIVED }
